class Solution {
    public int singleNumber(int[] nums) {
        //Edge Case
        if(nums.length == 0) return 0;
        
        //Variable
        int result = 0;
        
        //Main Logic
        for(int x:nums)
        {
            result ^= x;
        }
        
        //Result
        return result;
    }
}
